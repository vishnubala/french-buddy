node_modules/
dist/

# Secrets — never commit. generate-audio.mjs reads .env for the Azure key.
.env
.env.*
!.env.example

.DS_Store

# Local preview-harness config (machine-specific dev-server port) — not shared.
.claude/launch.json
